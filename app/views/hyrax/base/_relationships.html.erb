<% presenters = presenter.member_of_collection_presenters %>
<% unless presenters.blank? %>
  <h2><%= t('.header') %></h2>
  <table class="table table-striped relationships">
    <tbody>
      <%= render 'relationships_parent_rows', presenter: presenter %>
    </tbody>
  </table>
<% end %>
